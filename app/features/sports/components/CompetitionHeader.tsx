// ⚠️  DEPRECATED stub — was never implemented or imported.
// Competition screens build their own context-rich header inline (gradient + logo + badges).
// Do not use this component. It will be removed in a future cleanup pass.
export default function CompetitionHeader() {
  return null;
}

